import React, {useState} from "react";

import {Link} from "react-router-dom";

const Invalid = (props) => {

    return (
        <React.Fragment>
            <div>Trang web không hỗ trợ trên thiết bị này</div>
        </React.Fragment>
    );
};

export default Invalid;